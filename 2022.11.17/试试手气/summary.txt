Think： 可以将点数赋给变量，判断每个数是否为6，若不是则让它等于6，然后逐个递减，若为6，让他每次减1

summary：
1.在做的时候发现无法比较数组现在值和原来值是否重复，因此需要建立一个新数组来存放原来输入的值，并在后续循环中加一个判断条件;
2.如果相同的话就让他减2；
3.题给要求输出不能有多余的空格，因此在最后输出数组是需要加一个判断条件；
4.在第一次思路未考虑若数组中已经有6这个数据的时候，让n=6这个变量已经会减一，因此在第二个为6的数再次经历循环时，程序错误。