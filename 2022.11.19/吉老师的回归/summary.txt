1.使用 gets() 输入记得先用 getchar() 吸收回车；
2.在字符串中查找字符用 strstr 函数；
3.可以提前声明一个字符串 str[505]=“Wo AK le” ，然后只需要判断能否做完，如果做完，就把做完时的这道题，复制到 str
中；最后直接输出即可。
4.char *strcpy(char *dest, const char *src)
  	dest -- 指向用于存储复制内容的目标数组。
	src -- 要复制的字符串。
4.好难！